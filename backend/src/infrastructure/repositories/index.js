/**
 * =============================================================================
 * SNACKS BY LEBO - Infrastructure Layer: Repository Exports
 * =============================================================================
 */

const ProductRepository = require('./ProductRepository');
const OrderRepository = require('./OrderRepository');

module.exports = {
    ProductRepository,
    OrderRepository
};
